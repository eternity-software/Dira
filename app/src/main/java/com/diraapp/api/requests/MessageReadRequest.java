package com.diraapp.api.requests;

public class MessageReadRequest extends Request {
    public MessageReadRequest(long requestId, RequestType requestType) {
        super(requestId, requestType);
    }
}
